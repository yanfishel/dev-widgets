
export type T_DiskInfo = {
  diskLetter: string
  diskUse: number
  diskSize: string
  usedWidth: number
  errorWidth: number
}

export type T_MemoryInfo = {
  total: string
  used: string
}

export type T_MaxSpeed = {
  speed:number
  label: string
}